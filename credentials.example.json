{
  "_comentario": [
    "Copiá este archivo como credentials.json y completá las rutas.",
    "credentials.json está en .gitignore porque la contraseña del .p12 va en claro.",
    "Poné el .p12 y el .mobileprovision en certs/ (también ignorado por git).",
    "El perfil actual es ad-hoc, con App ID explícito app.carrot4037.persimmon1148",
    "y un solo dispositivo autorizado. app.json ya usa ese bundleIdentifier:",
    "si cambiás de perfil, hay que actualizar app.json para que coincida."
  ],
  "ios": {
    "provisioningProfilePath": "certs/perfil.mobileprovision",
    "distributionCertificate": {
      "path": "certs/certificado.p12",
      "password": "LA_CONTRASENA_DEL_P12"
    }
  }
}
